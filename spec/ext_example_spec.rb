require 'spec_helper'

describe ExtExample do
  it 'has a version number' do
    expect(ExtExample::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
